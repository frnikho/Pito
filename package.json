{
  "name": "pito",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "api:dev": "pnpm --filter api dev",
    "api:build": "pnpm --filter api build",
    "dashboard:dev": "pnpm --filter dashboard dev",
    "dashboard:build": "pnpm --filter dashboard build",
    "prisma:generate": "pnpm --filter db generate",
    "prisma:migrate": "pnpm --filter db migrate:dev",
    "prisma:validate": "pnpm --filter db validate",
    "types:build": "pnpm --filter types build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.0.4"
  }
}
